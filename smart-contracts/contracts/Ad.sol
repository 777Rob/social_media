
// contract Ad{
//     // Advertiser address
//     address public advertiser;

//     // Amount given for completing task
//     uint256 public taskReward;

//     // Total amount of tokens that will be distributed for the users that reviewd advertisment and creators they support
//     uint256  public rewardBalance;

//     // Advertisment status
//     bool public active;

//     // Reward for seeing the ad if
//     uint256 public viewReward;

//     bytes public task;

//   constructor(address _advertiser, uint256 _taskReward, uint256 _rewardBalance, uint256 _viewReward, bytes memory _task){
//     advertiser = _advertiser;
//     taskReward = _taskReward;
//     rewardBalance = _rewardBalance;
//     active = false;
//     viewReward = _viewReward;
//     task = _task;
//   }
// }
